<Window x:Class="consorApp.Views.GestionReclamosAdminView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ConsorApp - Administración de Reclamos" Height="520" Width="850"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ENCABEZADO -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Panel de Control de Reclamos" FontSize="22" FontWeight="Bold" Foreground="#2E363A"/>
            <TextBlock Text="Supervisión y seguimiento de los incidentes reportados en el consorcio" FontSize="13" Foreground="#666666" Margin="0,3,0,0"/>
        </StackPanel>

        <!-- FILTROS SUPERIORES -->
        <Border Grid.Row="1" Background="#F9F9F9" Padding="12" CornerRadius="6" BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Filtrar por Motivo:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox x:Name="CmbFiltroMotivo" Width="180" Height="25" Margin="0,0,20,0">
                    <ComboBoxItem Content="Todos los motivos" IsSelected="True"/>
                    <ComboBoxItem Content="Ruidos molestos"/>
                    <ComboBoxItem Content="Gotera / Filtración"/>
                    <ComboBoxItem Content="Basura fuera de horario"/>
                </ComboBox>

                <TextBlock Text="Estado:" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox x:Name="CmbFiltroEstado" Width="140" Height="25">
                    <ComboBoxItem Content="Todos" IsSelected="True"/>
                    <ComboBoxItem Content="Pendiente"/>
                    <ComboBoxItem Content="En proceso"/>
                    <ComboBoxItem Content="Resuelto"/>
                </ComboBox>
            </StackPanel>
        </Border>

        <!-- TABLA / DATAGRID PRINCIPAL -->
        <DataGrid x:Name="DgReclamosAdmin" Grid.Row="2" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding IdReclamo}" Width="50"/>
                <DataGridTextColumn Header="Motivo" Binding="{Binding Motivo}" Width="150"/>
                <DataGridTextColumn Header="Ubicación / Área" Binding="{Binding Ubicacion}" Width="130"/>
                <DataGridTextColumn Header="Inquilino / Unidad" Binding="{Binding Responsable}" Width="150"/>
                <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha}" Width="100"/>
                <DataGridTextColumn Header="Estado" Binding="{Binding Estado}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- BOTONES INFERIORES DE ACCIÓN -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="BtnEnResolucion" Content="Marcar En Resolución" Width="150" Height="32" Background="#F0AD4E" Foreground="White" FontWeight="Bold" Margin="0,0,10,0" Cursor="Hand"/>
            <Button x:Name="BtnMarcarResuelto" Content="Marcar como Resuelto" Width="170" Height="32" Background="#5CB85C" Foreground="White" FontWeight="Bold" Margin="0,0,10,0" Cursor="Hand"/>
            <Button x:Name="BtnVerDetalle" Content="Ver Detalle" Width="110" Height="32" Background="#A09285" Foreground="White" FontWeight="Bold" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>