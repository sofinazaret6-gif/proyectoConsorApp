<Window x:Class="consorApp.Views.DashboardView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:consorApp.Views"
        mc:Ignorable="d"
        Title="ConsorApp - Panel Principal"
        Height="720"
        Width="1200"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <!-- PALETA DE COLORES DEL LOGO -->
        <SolidColorBrush x:Key="ColorGrisOscuro" Color="#2E363A"/>
        <SolidColorBrush x:Key="ColorTaupeBronce" Color="#A09285"/>
        <SolidColorBrush x:Key="ColorFondoContenido" Color="#F7F5F2"/>
        <SolidColorBrush x:Key="ColorTextoSecundario" Color="#A0A0A0"/>

        <!-- ESTILO PARA LOS BOTONES DEL MENÚ LATERAL -->
        <Style x:Key="EstiloBotonMenu" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="20,0,0,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Margin="10,2">
                            <ContentPresenter VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3D474C"/>
                                <Setter Property="Foreground" Value="{StaticResource ColorTaupeBronce}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ESTILO BOTÓN CERRAR SESIÓN -->
        <Style x:Key="EstiloBotonSalir" TargetType="Button" BasedOn="{StaticResource EstiloBotonMenu}">
            <Setter Property="Foreground" Value="#E74C3C"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3D2020"/>
                    <Setter Property="Foreground" Value="#FF6B6B"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <!-- Columna 0: Menú Lateral (Sidebar) -->
            <ColumnDefinition Width="250"/>
            <!-- Columna 1: Áreas de Contenido -->
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ==================== MENÚ LATERAL (SIDEBAR) ==================== -->
        <Grid Grid.Column="0" Background="{StaticResource ColorGrisOscuro}">
            <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="70"/>
            </Grid.RowDefinitions>

            <!-- Encabezado / Logo -->
            <StackPanel Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock FontSize="24" FontWeight="Bold">
                    <Run Text="Consor" Foreground="White"/><Run Text="App" Foreground="{StaticResource ColorTaupeBronce}"/>
                </TextBlock>
                <Rectangle Width="60" Height="2" Fill="{StaticResource ColorTaupeBronce}" Margin="0,4,0,0"/>
            </StackPanel>

            <!-- Módulos / Opciones de Menú (Se ocultan/muestran en C#) -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="PanelMenuNavegacion">

                    <TextBlock Text="GENERAL"
                               Foreground="{StaticResource ColorTextoSecundario}"
                               FontSize="11" FontWeight="Bold"
                               Margin="20,10,0,5"/>

                    <Button x:Name="BtnAvisos" Content="📢  Avisos" Style="{StaticResource EstiloBotonMenu}"/>
                    <Button x:Name="BtnReclamos" Content="🛠️  Reclamos" Style="{StaticResource EstiloBotonMenu}"/>
                    <Button x:Name="BtnReservas" Content="📅  Reservas" Style="{StaticResource EstiloBotonMenu}"/>

                    <!-- SECCIÓN EXCLUSIVA ADMINISTRACIÓN Y ENCARGADO -->
                    <TextBlock x:Name="HeaderGestion" Text="GESTIÓN"
                               Foreground="{StaticResource ColorTextoSecundario}"
                               FontSize="11" FontWeight="Bold"
                               Margin="20,20,0,5"/>

                    <Button x:Name="BtnExpensas" Content="💰  Liquidación Expensas" Style="{StaticResource EstiloBotonMenu}"/>
                    <Button x:Name="BtnUsuarios" Content="👥  Gestión Usuarios" Style="{StaticResource EstiloBotonMenu}" Click="BtnUsuarios_Click"/>
                    <Button x:Name="BtnUnidades" Content="🚪  Departamentos" Style="{StaticResource EstiloBotonMenu}"/>

                </StackPanel>
            </ScrollViewer>

            <!-- Pie del Menú (Cerrar Sesión) -->
            <Border Grid.Row="2" BorderBrush="#3D474C" BorderThickness="0,1,0,0">
                <Button x:Name="BtnCerrarSesion"
                        Content="🚪  Cerrar Sesión"
                        Style="{StaticResource EstiloBotonSalir}"
                        VerticalAlignment="Center"
                        Click="BtnCerrarSesion_Click"/>
            </Border>
        </Grid>

        <!-- ==================== ÁREA DE CONTENIDO PRINCIPAL ==================== -->
        <Grid Grid.Column="1" Background="{StaticResource ColorFondoContenido}">
            <Grid.RowDefinitions>
                <RowDefinition Height="65"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Barra Superior de Usuario -->
            <Border Grid.Row="0" Background="White" BorderBrush="#E5E0DA" BorderThickness="0,0,0,1" Padding="25,0">
                <Grid VerticalAlignment="Center">
                    <TextBlock x:Name="TxtTituloSeccion" Text="Panel Principal" FontSize="18" FontWeight="Bold" Foreground="{StaticResource ColorGrisOscuro}"/>

                    <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                        <StackPanel VerticalAlignment="Center" Margin="0,0,10,0">
                            <TextBlock x:Name="TxtNombreUsuario" Text="Nombre Usuario" FontWeight="SemiBold" HorizontalAlignment="Right" Foreground="{StaticResource ColorGrisOscuro}"/>
                            <TextBlock x:Name="TxtRolUsuario" Text="Rol / Perfil" FontSize="11" Foreground="#888888" HorizontalAlignment="Right"/>
                        </StackPanel>
                        <!-- Indicador de avatar sencillo -->
                        <Border Width="36" Height="36" CornerRadius="18" Background="{StaticResource ColorTaupeBronce}" VerticalAlignment="Center">
                            <TextBlock Text="👤" Foreground="White" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="16"/>
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Panel de Contenido Variable (Aquí se cargarán las vistas o datos) -->
            <Grid Grid.Row="1" Margin="25">
                <Border Background="White" CornerRadius="10" BorderBrush="#E5E0DA" BorderThickness="1" Padding="25">
                    <StackPanel x:Name="PanelContenidoDinamico" VerticalAlignment="Center" HorizontalAlignment="Center">
                        <TextBlock Text="Bienvenido a ConsorApp" FontSize="22" FontWeight="Bold" Foreground="{StaticResource ColorGrisOscuro}" HorizontalAlignment="Center"/>
                        <TextBlock Text="Selecciona una opción del menú lateral para comenzar." FontSize="14" Foreground="#777777" Margin="0,8,0,0" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Window>